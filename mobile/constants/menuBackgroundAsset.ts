import type { ImageSourcePropType } from "react-native";

/** Home, group picker, online lobby/results (non-match), dev console, game-over — cycling uses other frames in-game only. */
export const STATIC_MENU_BG: ImageSourcePropType = require("../assets/bg/bg12.png");
