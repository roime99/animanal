# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
backend/.env

# Runtime SQLite (created on server from init_social_db)
backend/data/social.sqlite

# Node / Expo
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/web-build/

# Local secrets (copy from .env.example)
mobile/.env

# Never ship a local image pack in this edition (Wikimedia URLs only)
images/

# Generated from repo-root `bg/` + `fonts/` by `npm run vendor:assets` in mobile/
mobile/assets/bg/
mobile/assets/fonts/

# Font bundle / docs — keep only .otf/.ttf in git
fonts/*.zip
fonts/*.pdf

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
